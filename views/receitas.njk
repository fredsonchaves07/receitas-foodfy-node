{% extends 'layout.njk' %}
        {% block content %}
        <div class="receitas-mais-acessadas">
            {% for receita in dados %}
            <section class="receitas">
                <a href="/receitas/{{dados.indexOf(receita)}}"><img src="{{receita.image}}" alt="imagem do {{receita.title}}"></a>
                <p class="info">{{receita.title}}</p>
                <p class="autor">por {{receita.author}}</p>
            </section>
            {% endfor %}
            {% for receita in dados %}
            <section class="receitas">
                <a href="/receitas/{{dados.indexOf(receita)}}"><img src="{{receita.image}}" alt="imagem do {{receita.title}}"></a>
                <p class="info">{{receita.title}}</p>
                <p class="autor">por {{receita.author}}</p>
            </section>
            {% endfor %}
        </div>
    </main>
    {% endblock %}